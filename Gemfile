# frozen_string_literal: true

source "https://rubygems.org"

# gem "rails"

gem "debug"
gem "ox"
gem "aws-sdk-core", ENV.fetch("AWS_SDK_CORE_VERSION", ">= 3")
gem "aws-sdk-sqs", ENV.fetch("AWS_SDK_SQS_VERSION", ">= 1")
